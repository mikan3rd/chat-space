.chat
  %p.chat__user
    = message.user.name
  %p.chat__date
    = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  %p.chat__content
    = message.body
  %p.chat__image
    - if message.image.present?
      = image_tag(message.image)
